# Archived Fork Metadata

This directory is a historical snapshot of the retired backend's upstream
patch queue. It is kept for migration archaeology only. The associated
automation has been removed and these patches are not replayed, tested, or
shipped by any supported workflow.

Current production development belongs in `backend-rust/` and must remain
PostgreSQL-only.
